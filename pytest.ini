[pytest]
addopts = -v -s -p no:cacheprovider --import-mode=importlib
pythonpath = .
markers =
    smoke: Run smoke tests
    factor_library_api: Run Factor Library API tests
    live_db: Run live DB-backed API consistency tests
testpaths = tests
